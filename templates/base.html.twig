<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>{% block title %}OVSchool | {% endblock %}</title>
    <link rel="icon"
          href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">


    {% block stylesheets %}
    {% endblock %}
</head>
<body style="background-image: url('{{ asset('image/fondRouge.jpg') }}');">
<header class="background" style="background-image: url('{{ asset('image/street.jpg') }}');">
    <nav class="navbar navbar-expand-lg navbar-dark">
        <!-- Auth links -->
        <div class="auth-nav bg-glass">
            {% if app.user %}
                <a class="nav-link text-white" href="{{ path('app_logout') }}">
                    <i class="fas fa-sign-out-alt"></i> Déconnexion
                </a>
            {% else %}
                <a class="nav-link text-white" href="{{ path('app_login') }}">
                    <i class="fas fa-sign-in-alt"></i> Connexion
                </a>
                <a class="nav-link text-white" href="{{ path('user_register') }}">
                    <i class="fas fa-user-plus"></i> Inscription
                </a>
            {% endif %}
        </div>

        <!-- Burger menu button -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Main content -->
        <div class="container-fluid centrerText">
            <div>
                <a class="navbar-brand titleSite neon-box" href="#">OVSchool</a>
            </div>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ">
                    <li class="nav-item">
                        <a class="nav-link active SousTitre" aria-current="page"
                           href="{{ path('main_home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('sortie_list') }}">Sorties</a>
                    </li>
                    {% if app.user %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('user_detail', {'id': app.user.id}) }}">Profil</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link" href="#">Plus</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>

<main>


        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="{{ label }}">{{ message }}</div>
            {% endfor %}
        {% endfor %}



        {% block body %}{% endblock %}
</main>
<footer>

</footer>

{% block javascripts %}
    {% block importmap %}{{ importmap('app') }}{% endblock %}
{% endblock %}

</body>

</html>
